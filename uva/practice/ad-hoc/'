#include <algorithm>
#include <iostream>

int main() {
    std::ios_base::sync_with_stdio(false);
    while (true) {
        int cards[53] = {0};
        int A, B, C, X, Y, start_card = 1;
        std::cin >> A >> B >> C >> X >> Y;
        if (A == 0)
            break;
        cards[A] = 1, cards[B] = 1, cards[C] = 1, cards[X] = 1, cards[Y] = 1;
        if (std::max(A, std::max(C, B)) < std::min(X, Y)) {
            start_card = 1;
        } else {
            // We need at least two wins
        }
        for (int i = start_card; i <= 52; i++)
            if (cards[i] != 1) {
                std::cout << i << "\n";
                break;
            }
    }
}
