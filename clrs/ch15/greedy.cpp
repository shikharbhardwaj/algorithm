#include <vector>
std::vector<int> prices;
int length;
int accumulate_rev(int i){
  if( i <= 0 ){
    return 0;
  }
  return 0;
}
int main(){
  prices = {0,3,4,2,5,1,9,12,6,13,10};
  length = 10;
  accumulate_rev(1);
}
