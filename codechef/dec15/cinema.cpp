#include <iostream>
int main(){
  std::ios_base::sync_with_stdio(false);
  int T, N, L, R, B, Z, M;
  std::cin>>T;
  while(T--){
    std::cin>>N>>M>>Z>>L>>R>>B;
    //Find the max number of people that can 
    //attend the show
    //
    //First keep all the people with the same preference
    //together. The end seats are kept for the dualies
   } 
}

