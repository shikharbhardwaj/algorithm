// Upsolve solution to : MARCH16/SEASTR2
// The question asks to find the ordered pairs (a,b) such that a cannot be
// transformed to b with at most 2 char swaps
//
// The total number of permutation is n!, if all the chars are diff
// The total number ordered pairs then becomes n!*(n-1)! - 2*n
// Hence the answer then becomes n!*(n-1)!
//
// The problem is where there are similar chars in the string
// We neeed to make cases
#include <bits/stdc++.h>
int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(0);
    int T;
    std::cin >> T;
    while (T--) {
    }
}
