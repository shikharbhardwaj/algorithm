#include <bits/stdc++.h>
using namespace std;
using LL = long long;
int main() {
    ios_base::sync_with_stdio(false);
    int T;
    cin >> T;
    while (T--) {
        int n, m, D, d;
        std::cin >> n >> m >> d >> D;
        if (m >= n * d && m <= n * D) {
            int p = m / n;
            int e = 0;
            while (true) {
            }
        } else {
            std::cout << "-1\n";
        }
    }
}
